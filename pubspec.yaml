name: iot
description: "I dont wannt this app"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter

  js: ^0.6.7
  audioplayers: ^5.2.1
  geolocator: ^10.1.0
  url_launcher: ^6.2.5
  shared_preferences: ^2.1.0
  cupertino_icons: ^1.0.8
  permission_handler: ^11.3.0
  flutter_blue_plus: ^1.30.4
  flutter_local_notifications: ^17.1.0
  lottie: ^3.1.0
  firebase_core: ^3.13.1
  firebase_messaging: ^15.2.6
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/animations/
    - assets/sounds/alarm.wav